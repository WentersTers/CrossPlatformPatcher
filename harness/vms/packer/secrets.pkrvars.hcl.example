# Copy to secrets.pkrvars.hcl (gitignored, NEVER committed) and fill in:
#   cp secrets.pkrvars.hcl.example secrets.pkrvars.hcl
# Generate the hash ON THE HOST:  mkpasswd -m sha-512
ssh_username  = "sage"
ssh_password  = "CHANGEME"
password_hash = "CHANGEME-paste-mkpasswd-sha512-output-here"
